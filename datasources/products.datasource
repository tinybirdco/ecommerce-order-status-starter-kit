
SCHEMA >
    `id` Int64,
    `name` String,
    `department` String,
    `material` String,
    `brand` String,
    `size` String,
    `price` Float32

ENGINE "MergeTree"
ENGINE_SORTING_KEY "id, material, brand, size"
